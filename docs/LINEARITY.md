# Inline Linearity Contract

This file is the standing O(n) ownership map for inline-phase forward scans. Each scan
must have exactly one owner: constant/local, consume-on-match, suffix-absence miss-cache,
invalidating cursor, precomputed map, or boundary-run map. A new unfloored scan in these
paths is a bug. Every new row must state the summation argument, not only name the mechanism.
Audit lesson: counters must charge data-structure work, and every cache key dimension needs a
gate shape that maximizes its cardinality. Product rule for block frames: every frame kind ×
multi-line construct pair needs an adjacent-siblings gate shape.

C8 refreshed the driver rows after the dispatch loops were rewritten in `inline_choices`
order. Older construct-internal rows are kept where the owner did not change.

## Scan-Owner Census

Every committed scan site must have a written owner. The source annotation grammar is:

```text
// scan-owner: <class> — <short owner/why>
```

The class vocabulary is:

- `(a)` consumed-on-match work, including accepted copies.
- `(a2)` caller-owned helper work over the caller's current slice/range.
- `(b)` monotone cursor, miss-cache, path-compressed memo, or per-buffer precompute owner.
- `(c)` constant/current-slice bounded work with no unbounded retry path.
- `(o)` output-size work outside parser complexity accounting.
- `(d)` unowned repeat probe; this class is never committed.

The verified Phase 1 table is `subagent-tasks/notes/scan-loop-census.md`. The deterministic
gate depends on charged work: the F6-F8 lesson is that fully uncharged loops are invisible to
`scan_work` in both directions, so they can neither prove linearity nor expose growth. Parser
work that scans or copies input-derived bytes must be charged in debug builds; `(o)` and
constant/N/A rows do not add parser charges.

| scan @ file:line | owner | argument |
|---|---|---|
| Shared fresh/swallow tables @ `src/inline_driver.rs:17` | constant tables | Markdown/Org plain-delimiter and swallow-byte tables encode mldoc `plain` fallback; no scan is introduced. |
| Inline lexers @ `src/lexer.rs:98,120`, `src/org_resolver.rs:145,168` | consume-on-match | `i` advances monotonically over the input; the pending-plain `push_str` and the whitespace-run `to_string` each copy input bytes at most once (charged to `scan_work`). Σ copies ≤ n. |
| Resolver pending-plain buffers @ `src/resolver.rs:1266-1298`, `src/org_resolver.rs:956-993` | consume-on-match | `append_transformed`/`append_text`/`append`/`push_byte` copy each accepted plain/transformed byte into `pending` once; charged. Σ ≤ n over the disjoint accepted spans. |
| Plain transform helpers @ `src/resolver.rs:305` (`markdown_plain_text`), `src/resolver.rs:333`/`src/org_resolver.rs:385` (`cr_plain_origins`), `src/resolver.rs:2123` (`truncate_plain_origins`) | bounded by the plain node's slice | each is one pass over a plain node's own (disjoint) text/segments; charged by slice length. Σ over all plain nodes ≤ n. |
| Top-level plain-inline fast path @ `src/inline.rs` | consumed current inline buffer or early ownership decline | `plain_fast_path_markdown` and `plain_fast_path_org` classify each byte once until either an unowned construct-start byte declines to the full resolver or the whole buffer is accepted. A direct plain/break-only builder runs before allocating resolver scan state: it accepts only bytes that cannot open any inline construct, treats non-URL `:` as plain, emits Markdown hardbreaks with the same current-suffix rule, and declines at the first construct-start byte so the fused scanner still owns mixed buffers. The plain-run skipper advances over bytes whose classes cannot open a construct and charges that skipped slice once; accepted plain slices are copied once into `Plain` nodes. Whitespace runs are consumed as one run while leaving `fresh = true` for the next non-space byte, so tag/URL/timestamp freshness is unchanged. Markdown hardbreak detection scans only the current pending plain suffix immediately before its newline. The fast path also accepts source-transcribed Logseq tag leaves in both formats, fresh common bare URLs (`http://`, `https://`, `ftp://`, `file://`) in both formats through the shared `parse_bare_url_with_scan`/`BareUrlScan` owner after a constant prefix check, fresh keyword/range timestamps through the shared `TimestampCloseScan` before later angle-date dispatch, fresh top-level Logseq macros/block refs through the shared `parse_macro_at`/`parse_block_ref_at` parsers, Markdown `[` dispatch in mldoc order through shared owners (`FootnoteRefScan`, `PageRefScan`, `MdLinkScan` including its monotone raw `](` floor, `TimestampCloseScan`, statistics-cookie local scan, and sparse inline hiccup close index), Org `[` dispatch in mldoc order through shared owners (`OrgInlineScan`, `TimestampCloseScan`, statistics-cookie local scan, and sparse inline hiccup close index), Markdown known alphabetic entities through the same table as the lexer, Markdown ASCII-punctuation escapes as source-mapped transformed Plain nodes, Markdown `$...$`/`$$...$$` LaTeX through the shared dollar parser after a monotone same-line `$` presence check, Markdown fresh `<...>` angle dispatch through `resolver::try_markdown_angle_at` in mldoc order (`quick_link`, timestamp, inline HTML, email) with `AutolinkScan`, `TimestampCloseScan`, `RawHtmlScan`, and `EmailAutolinkScan`, and Org fresh `<...>` angle dispatch through `org_resolver::try_target_angle_at` in mldoc order (`quick_link`, target, radio target, timestamp, inline HTML, email) with `OrgInlineScan`, `AutolinkScan`, `TimestampCloseScan`, `RawHtmlScan`, and `EmailAutolinkScan`; those accepted leaves consume their own spans and share their normal monotone/precomputed owners. The fast path delegates `\(`/`\[` and unknown alphabetic backslash escapes to the resolver so latex precedence and the unknown-entity `{}` quirk stay in the source-transcribed path. The fused scanner mirrors the resolver's `fresh` state for accepted URL, keyword timestamp, macro, block-ref, Markdown angle, Markdown bracket, Markdown backslash entity/escape, Markdown LaTeX, Org bracket, Org angle, and Org code/verbatim starts: buffer start, whitespace, breaks, accepted constructs, Markdown delimiter-byte fallbacks such as single `=`, failed backticks, failed `$`, failed emphasis opener bytes, failed `[`, and identifier `_`; swallowed punctuation such as non-fresh `!`, `{`, `(`, or `<` does not open those constructs. Other `://` forms, including mldoc's `c`/`d`/`s` protocol exclusion and uncommon protocols, decline to the resolver rather than adding a per-word scan. Markdown fresh `![alt](url)` images delegate to the same `md_link_with_scan`/`MdLinkScan` owner as the resolver; non-fresh `!` stays plain so the following `[` can still become an ordinary link. Markdown identifier underscores whose previous byte is ASCII alphanumeric and whose next byte is not `{` stay plain by the same `underline_emphasis_delims_backward` and script-open gates used by the full resolver, and single `=` bytes stay plain because mldoc's Markdown highlight opener is only `==`; opener/script-capable underscores and `==` still decline unless the source-transcribed braced-script helper first proves emphasis would not win and then accepts `_{...}`/`^{...}` through the resolver's script parser. Markdown code spans are accepted through the shared `lexer::code_span` helper, and the optional CR normalization scans only that accepted code text. Markdown `*`, `_`, `^`, `~~`, and `==` emphasis candidates call the resolver's source-transcribed `nested_emphasis` helper after the narrow `**...**` shortcut, sharing one no-closer table and one terminal-backslash result for the inline buffer; failed opener bytes are swallowed as literal text instead of forcing a fallback. The shortcut disables tag/macro/block-ref leaves inside emphasis bodies to match mldoc nested-plain repair. Org fresh `=...=`/`~...~` code/verbatim and non-stateful `*...*`/`^^...^^` emphasis candidates call the corresponding Org resolver helpers. Accepted bodies consume disjoint spans, while failed candidates either swallow the same literal byte the resolver would swallow or decline immediately to the resolver if the construct family is still unowned. Rejected mixed-markup buffers pay at most the prefix before the first unowned special byte, then the existing resolver owns the full parse. |
| Markdown delimiter-state fast path @ `src/inline.rs`, `src/resolver.rs` | O(1) state update + shared no-closer table | The fused fast path mirrors resolver `pending.last().or(last_plain_char)` for `_`/`^` delimiter attempts. Plain-run skips and literal fallback bytes update `last_plain_char` in O(1), accepted non-plain leaves do not, and the same cached Markdown nested-emphasis helper owns forward closer searches. This removes the prior conservative fallback after accepted leaves without introducing another scan. |
| Org slash/plus fast-path emphasis @ `src/inline.rs`, `src/org_resolver.rs` | consumed delimiter byte + shared no-closer table | The fused plain fast path mirrors Org resolver `last_plain_char` with O(1) updates when it consumes plain runs or literal opener fallbacks; accepted non-plain leaves intentionally do not update that state. `/` and `+` delimiter attempts call the source-transcribed Org nested-emphasis parser with that state, one shared `no_closer[class][k]` table, and one terminal-backslash result for the current inline buffer. Accepted bodies consume disjoint spans; failed `*`, `/`, and `+` opener bytes are swallowed as literal plain, while failed `^^` still declines to the script-aware resolver path. |
| Markdown resolver token loop @ `src/resolver.rs:1259` | consume-on-match | `t` advances monotonically; successful leaves resync past consumed bytes. |
| Inline OriginMap build/remap @ `src/parse.rs:1717`, `src/org.rs:1766`, `src/source_map.rs:299` | compose-at-build + one monotone remap | Fold buffers append source views and EOL joiners once while constructing text; remap walks each inline run and each origin segment in source order, with no suffix rescans or endpoint-only remapping. |
| GtFallback parent OriginMap remap @ `src/parse.rs:329`, `src/org.rs:444`, `src/source_map.rs:368` | one cursor per parent map traversal | A fallback reparse owns one parent-origin cursor for direct paragraph/term/table-cell remaps and fallback-owned child-map construction. Children keep separate cursors for their own `OriginMap`s, and inline recursion snapshots the pre-parent cursor before remapping child spans. Summation: parent cursor advances at most the fallback map's segment count plus O(fragments); child maps are traversed once each. |
| Parser-owned transformed buffer copies @ `src/parse.rs:1769`, `src/org.rs:1780`, `src/org.rs:2400` | charged copy-on-build | Paragraph/list/definition/footnote/block-code buffers append each viewed source byte and synthetic newline once while building the transformed text. `OriginMap::copy_interval_into` charges each copied origin segment, so total copy work is bounded by copied bytes plus copied segment count. |
| Block displayed-math/LaTeX/fence/hiccup payload copies @ `src/parse.rs:781`, `src/parse.rs:1649`, `src/org.rs:1014`, `src/org.rs:1664` | consume-on-match + charged copy | Each accepted block payload is copied only for its accepted extent; adjacent siblings consume disjoint extents, and transformed-view reconstruction appends each viewed line and joiner once. |
| Callout closer trie @ `src/block_common.rs:1648`, `src/block_common.rs:1689` | charged trie walk + monotone cursors | `EndTrie::insert`/`find` charge every suffix/name byte, child-vector comparison, and `ends` cursor advance. For a parse pass, each trie-node cursor advances monotonically over its sorted closer list, so total lookup work is O(name bytes + child comparisons + closer events). |
| Markdown page-ref `]]` lookup @ `src/resolver.rs:1311` | precomputed-map | `build_real_dbl` positions are shared with a monotone cursor. |
| Markdown hiccup/nested close lookup @ `src/resolver.rs:1301`, `src/resolver.rs:1306` | precomputed-map | `build_hiccup_close` and `build_nested_close` give O(1) close checks. |
| Markdown md-link coarse `](`/`)` floors @ `src/resolver.rs:1316`, `src/resolver.rs:2606`, `src/inline.rs` | invalidating-cursor + md-link scan owner | Resolver `lbp_cur`, `crlf`, and `rparen` advance only forward before `md_link_with_scan`; they prove only candidate presence. The shared `MdLinkScan` also owns a monotone raw `](` absence floor, so top-level fused inline parsing does not run the full label validator for every `[` when no Markdown link delimiter exists in the buffer. Repeated label/URL validation work is owned by the per-inline-buffer `MdLinkScan` rows below. |
| Markdown emphasis body parser @ `src/resolver.rs:255`, dispatch @ `src/resolver.rs:432` | consume-on-match + suffix-absence miss-cache | mldoc `md_em_parser` consumes each body byte once per bounded nesting phase; `no_closer[class][k]` floors EOF/no-closer failures. |
| Markdown script braced scan @ `src/resolver.rs:1229`, body reparse @ `src/resolver.rs:922` | `}`-before-eol invalidating-cursor + bounded body | Braced `_`/`^` dispatch is gated by `ByteBeforeEolScan` at label, nested, and top-level call sites; accepted script bodies are disjoint consumed spans and body reparse is bounded by that span. |
| Markdown entity lexer @ `src/lexer.rs:173` | consume-on-match | `\` plus the maximal ASCII-letter run and optional `{}` are consumed once; known names emit `Entity`, unknown names emit the same consumed bare name as `Plain`. |
| Markdown tag dispatch @ `src/resolver.rs:1293`, `src/inline.rs:573` | boundary-run | delimiter-run termination is precomputed once by `build_tag_boundary_runs`. |
| Markdown macro dispatch @ `src/resolver.rs:1573`, `src/inline.rs:2223` | suffix-absence miss-cache + invalidating-cursor | `}}` floor proves close presence; first lone `}` cursor prevents repeated invalid misses. |
| Markdown export-snippet dispatch @ `src/resolver.rs:1609`, parser @ `src/inline.rs:2266` | suffix-absence miss-cache + consume-on-match | `@@` closer floor skips parser calls when no closer exists after the opener; accepted snippets consume through the closing `@@`, and invalid candidates scan only to the first `@`/EOL. |
| Markdown block-ref dispatch @ `src/resolver.rs:1666`, `src/inline.rs:2188` | suffix-absence miss-cache + invalidating-cursor | `))` floor proves close presence; first lone `)` cursor owns body-invalid failures. |
| Markdown angle autolink @ `src/resolver.rs:1871`, `src/inline.rs:1473` | suffix-absence miss-cache + invalidating-cursor | one cursor owns first `>`/ws after `<scheme:`; EOF and ws-before-`>` are cached outcomes. |
| Markdown angle timestamp @ `src/resolver.rs:1878`, `src/inline.rs:2316` | suffix-absence miss-cache + invalidating-cursor | active `<...>` bodies and active range halves are gated by the `>` half of `TimestampCloseScan`. |
| Markdown inactive bracket timestamp @ `src/resolver.rs:1487`, `src/inline.rs:2343` | suffix-absence miss-cache + invalidating-cursor | inactive `[...]` bodies and inactive range halves are gated by the `]` half of `TimestampCloseScan`, after link/reference attempts. |
| Markdown footnote refs @ `src/resolver.rs:1967`, `src/inline.rs:1334` | per-inline-buffer stop-class cursor | `[^...]` probes ask one monotone cursor for the first `]`, space, tab, form-feed, LF, or CR at/after the id start; SUB (`0x1a`) is ordinary content. Empty ids and whitespace stops still fail by the existing post-check, successes consume through `]`, and repeated no-close probes share one charged scan frontier per resolve/sub-buffer. |
| Markdown keyword/range timestamp @ `src/resolver.rs:1155`, `src/inline.rs:2330` | suffix-absence miss-cache + invalidating-cursor | keyword active/inactive bodies reuse the delimiter-specific timestamp cursor; range second-half probes are transactional clones, committed only on a successful range so fallback stays O(n). |
| Markdown email local/domain @ `src/resolver.rs:1890`, `src/inline.rs:1568` | suffix-absence miss-cache + invalidating-cursor | local-part keeps the `@` absence floor; domain uses a first `>`/ws cursor. |
| Markdown raw HTML angle @ `src/resolver.rs:1871`, `src/resolver.rs:2351`, `src/block_common.rs:806` | suffix-absence miss-cache + precomputed-map | Missing closers use `RawHtmlScan`; unbalanced tag matching uses the parse-local raw-HTML tag index below, so total work is one charged per-tag NSE build plus amortized cursors and O(1) fit checks. |
| Markdown bare URL dispatch @ `src/resolver.rs:1161`, `src/inline.rs:1749` | consume-on-match + suffix-absence miss-cache | accepted URLs consume their span; all-alphanumeric no-scheme suffixes are floored by `BareUrlScan`. The mldoc keyword-leading exclusion for `c`/`d`/`s` protocols is a constant dispatch guard before the protocol scan. |
| Markdown resync lead checks @ `src/resolver.rs:1812` | invalidating-cursor | split-token bare-url probes share `BareUrlScan`; fast path re-lexes only the split token. |
| Org resolver token loop @ `src/org_resolver.rs:953` | consume-on-match | `t` advances monotonically; accepted leaves resync past consumed bytes. |
| Org bracket/page/link floors @ `src/org_resolver.rs:806`, `src/org_resolver.rs:1083`, `src/org_resolver.rs:1786` | precomputed-map + invalidating-cursor | bracket close maps plus `rbracket`, `sq_rb_lb`, `real_dbl_cur`, and `crlf` gate scans. |
| Org macro/block-ref dispatch @ `src/org_resolver.rs:1167`, `src/org_resolver.rs:1241` | suffix-absence miss-cache | `sq_rbrace` and `sq_rr` must be present before parsers run. |
| Org export-snippet dispatch @ `src/org_resolver.rs:1187`, parser @ `src/inline.rs:2266` | suffix-absence miss-cache + consume-on-match | `sq_at` proves a closing `@@` after the opener before the parser runs; accepted snippets consume through the closer, failed candidates are bounded by the first `@`/EOL. |
| Org tag dispatch @ `src/org_resolver.rs:974`, `src/inline.rs:573` | boundary-run | same delimiter-run precompute as Markdown. |
| Org angle target @ `src/org_resolver.rs:1463`, `src/org_resolver.rs:2010` | shared `>`-before-eol invalidating-cursor | The target branch runs before the radio branch for byte-exactness. Both `<<...>>` and `<<<...>>>` query the same monotone `ByteBeforeEolScan(b'>')`, whose real stop class is `>` or CR/LF only; `<` inside the body is ordinary content. Queries are forward (`i+2`, then `i+3`, then later dispatch offsets), so the shared cursor scans each line suffix at most once while existing non-empty and double/triple-`>` post-checks preserve edge behavior. |
| Org autolink @ `src/org_resolver.rs:1647`, `src/org.rs:2210` | suffix-absence miss-cache + invalidating-cursor | shared first `>`/ws cursor gates `parse_org_autolink`. |
| Org angle timestamp @ `src/org_resolver.rs:1693`, `src/inline.rs:2316` | suffix-absence miss-cache + invalidating-cursor | active `<...>` bodies and active range halves are gated by the `>` half of `TimestampCloseScan`. |
| Org inactive bracket timestamp @ `src/org_resolver.rs:1833`, `src/inline.rs:2343` | suffix-absence miss-cache + invalidating-cursor | inactive `[...]` bodies and inactive range halves are gated by the `]` half of `TimestampCloseScan`, after org link attempts. |
| Org keyword/range timestamp @ `src/org_resolver.rs:890`, `src/inline.rs:2330` | suffix-absence miss-cache + invalidating-cursor | keyword active/inactive bodies reuse the delimiter-specific timestamp cursor; range second-half probes are transactional clones, committed only on a successful range so fallback stays O(n). |
| Org raw HTML angle @ `src/org_resolver.rs:1293`, `src/org_resolver.rs:1888`, `src/block_common.rs:806` | suffix-absence miss-cache + precomputed-map | Same `RawHtmlScan` and unbalanced tag index as Markdown: one charged per-tag NSE build per input string plus amortized cursors and O(1) fit checks. |
| Org email domain @ `src/org_resolver.rs:1709`, `src/inline.rs:1568` | suffix-absence miss-cache + invalidating-cursor | same email `@` floor plus domain boundary cursor as Markdown. |
| Org bare URL dispatch/resync @ `src/org_resolver.rs:896`, `src/org_resolver.rs:1568`, `src/inline.rs:1749` | consume-on-match + suffix-absence miss-cache | accepted URLs consume; bounded split-token resync probes share `BareUrlScan`. The shared `c`/`d`/`s` keyword-leading exclusion is constant work. |
| Org emphasis body parser @ `src/org_resolver.rs:370`, dispatch @ `src/org_resolver.rs:486` | consume-on-match + suffix-absence miss-cache | Org uses the same mldoc body parser with `include_md_code=false`; `no_closer[class][k]` floors EOF/no-closer failures. |
| Org script braced scan/fallback @ `src/org_resolver.rs:933`, body reparse @ `src/org_resolver.rs:1360` | `}`-before-eol invalidating-cursor + consume-on-match | Braced `_`/`^` attempts are gated by `ByteBeforeEolScan`; when absent, Org falls straight to the p1 `non_space` run. Accepted braced or p1 spans bound the body reparse. |
| Org entity dispatch @ `src/org_resolver.rs:1380`, top-level @ `src/org_resolver.rs:1477` | consume-on-match | Entity handling consumes the maximal ASCII-letter run and optional `{}` for both known and unknown names; unknown names consume the run and emit bare `Plain(name)`. |
| Shared page-ref walk @ `src/inline.rs:424`, `src/inline.rs:736` | per-buffer successor memo + charged raw fallback | `PageRefScan` memoizes the memoryless page-ref walk from classified byte positions: CR/LF fails, raw `]]` succeeds, `\` skips one byte without recording the skipped target, and all other bytes advance by their width. Each newly classified position is charged and written once; later repeat probes inside tags, emphasis reparses, URL pieces, and macro args are O(1). Direct top-level calls that are already gated by `real_dbl`/CRLF stay raw but charge their single consumed scan. |
| Shared nested-link matcher @ `src/inline.rs:811`, `src/inline.rs:424` | `]]`/LF successor memo + accept memo | The expensive `match_brackets` inner search is a position-pure next-raw-`]]`-or-LF walk owned by `PageRefScan`; each byte position in that walk is classified once. The outer level arithmetic and `[[` counting are charged over each accepted chunk. `parse_nested_link` also memoizes accept/reject plus end per opener position, so a successful bracket match later rejected by child-count is not recomputed. Top-level resolver calls remain protected by precomputed close maps. |
| `parse_tag_name` capture/reparse @ `src/inline.rs:913`, `src/inline.rs:930` | boundary-run + `PageRefScan` | Captured tag bytes advance monotonically; delimiter suffixes use the boundary map. Page-ref capture uses the outer buffer's `PageRefScan`, and the raw-CR/LF span check is narrowed to a forward `next_crlf_at_or_after(i)` query after a debug assertion that `[start..i)` is EOL-free. Capture calls query nondecreasing positions within one buffer pass, so the cursor scans each byte at most once; the bounded tag string is reparsed with a fresh scan owner. |
| Tag nested/page/link reparse @ `src/inline.rs:1010`, `src/org_resolver.rs:933` | fresh sub-buffer scan + consume-on-match | Each captured tag string owns a fresh `MdLinkScan` or `OrgInlineScan`, each embedding a fresh `PageRefScan`; no memo crosses buffer identity. Failed `[[` probes share path-compressed page/nested/org-link walks, and successful nested/page/Markdown/Org links advance the tag reparse cursor over disjoint spans. |
| Macro arg scans @ `src/inline.rs:1223`, `src/inline.rs:1261` | fresh macro-buffer `PageRefScan` | Macro parsing owns one fresh scan for the accepted macro argument string. Comma-separated args probe distinct `[[` and `((` positions in the same buffer: page-ref and nested-link failures share the path-compressed walk, and block-ref arg probes share a monotone first-`)` cursor with no comma cap and no EOL cap. The full-document `{{...}}` scanners stop outer macro bodies at CR/LF before calling the helper, but the helper contract itself scans through newlines. Ordinary quoted/plain arg scans are bounded by the accepted macro body. |
| Markdown `MdLinkScan` build @ `src/inline.rs:1341`, `src/inline.rs:1434`, `src/inline.rs:1458`, `src/inline.rs:1481`, owned by `src/resolver.rs:1481` | per-inline-buffer maps + `PageRefScan` | The raw `](` floor is a monotone `BytePairScan`: absence crosses each suffix region once, and raw hits inside escapes/code spans are only harmless false positives before full validation. The label bracket table and URL paren table encode the exact balanced-helper end query for every byte: fresh-start escape tokenization, `other_delims`, excluded-ending lookahead, and first below-baseline right. Label page-ref and code-span end tables answer label validation probes once per buffer; URL-piece page-ref and nested reparse probes use the embedded `PageRefScan`. Each table or successor chain is built at most once per inline buffer and charged O(buffer bytes), so failed opener attempts share one owner. |
| Markdown `markdown_embed_image` data branch @ `src/inline.rs:1245` | consume-on-match under md-link floors | The `data:` scan runs only after label validation succeeds and `try_md_link` proves a `)`; success consumes through that `)`, and failure is bounded by the same attempted link extent. |
| Markdown `label_part` validation/materialization @ `src/inline.rs:1321`, `src/inline.rs:1336`, `src/inline.rs:1389` | `MdLinkScan` + materialize-after-success | `label_part_url_start` first asks the shared raw `](` floor; if no such delimiter exists, the full validator does not walk the candidate label. `try_md_link` may call `label_part` for many openers sharing one `](`, but failed attempts perform only O(1) table queries plus local cursor jumps. Label nodes and copied label text are built only after `label_part_url_start` has found a delimiter; accepted links consume disjoint spans. Summation: one O(n) scan-owner build per inline buffer + O(number of attempted openers) lookups + Σ accepted label bytes. |
| Markdown label code spans @ `src/inline.rs:1138`, `src/inline.rs:1409` | `MdLinkScan` code-span end table | Single- and double-backtick label code end positions are precomputed once from next-backtick/next-EOL/next-double-backtick arrays. Querying a code opener during failed label validation is O(1), so the `[`/backtick interleave family charges only the shared table build plus attempted opener count. |
| Markdown `link_url_part` @ `src/inline.rs:1669` | `MdLinkScan` URL paren table + borrowed range | The URL query returns a borrowed byte range and end index, including the historical trailing-`)` fallback. The raw URL `String` is materialized and charged only for an accepted link; accepted URL spans are disjoint. |
| Markdown `link_url_part_inner` URL/title reparse @ `src/inline.rs:2061`, `src/inline.rs:2115` | borrowed accepted URL range + `MdLinkScan` | URL pieces are parsed over the original buffer range returned by `link_url_part_range`, so repeated page-ref probes in one URL use the outer `MdLinkScan`'s `PageRefScan` rather than a copied string. Quoted-title and non-page-ref piece scans are bounded by that accepted URL range; the raw URL `String` is materialized only after the range is accepted. |
| Markdown label Plain reparse @ `src/resolver.rs:67` | bounded disjoint label spans | Each Plain label node is reparsed once with the C1 emphasis port plus latex/entity/code/script choices; consume-all failure keeps that one Plain chunk, so chunks are not rescanned. |
| Markdown link metadata @ `src/inline.rs:2194`, owner `MdLinkScan` @ `src/inline.rs:1341` | per-inline-buffer CR/LF `}` absence floor | `{...}` metadata is checked immediately after an accepted normal link or `data:` embed image. Missing `}` scans only to the next CR, LF, or EOF and records that current-line absence in the buffer's `MdLinkScan`; metadata calls query nondecreasing positions within one buffer pass, so later accepted links on the same line query the floor in O(1). Success consumes through `}` and accepted metadata spans are disjoint. Summation: one charged missing-`}` scan per line/buffer + O(accepted links) lookups + Σ accepted metadata bytes. |
| Org `org_link_1` URL delimiter scan @ `src/org_resolver.rs:2257`, owner `OrgInlineScan` @ `src/org_resolver.rs:93` | per-position successor memo | The URL-side scan for the first fresh-start unescaped `]` is a position-pure chain: `\` skips exactly the next char, `]` stops, all other chars advance by `char_len`. `url_rbracket_memo[j]` stores that exact fresh-start result for every newly visited byte position, charged once. Summation: repeated `[[` openers sharing one late `][` pay one O(buffer) memo fill plus O(openers) lookups. |
| Org `org_link_1` label successor scan @ `src/org_resolver.rs:2355`, owner `OrgInlineScan` @ `src/org_resolver.rs:93` | per-position successor memo | Each label scan step at byte `j` is memoryless: final `]]`, plain-run end, balanced chunk end, single `]`, or char advance depends only on `j`. `label_end_memo[j]` stores the exact fresh-start outcome (`Some(close)` or no close), and each newly computed chain node is charged once before being memoized. Summation: every chain position in an inline buffer is newly computed at most once; later opener attempts are O(1) memo hits. |
| Org balanced label chunks @ `src/org_resolver.rs:2426`, owner `OrgInlineScan` @ `src/org_resolver.rs:93` | lazy per-`[` chunk-end memo | `chunk_end_memo[q]` stores the exact `org_balanced_label_chunk` result for `[` positions. A newly computed chunk scan charges the bytes it skips and records the end for the starting opener and every interior opener classified outside escapes; unmatched openers record EOF. Summation: each classified `[` in a label buffer receives a chunk end once, so overlapping failed chunks share one charged scan frontier instead of rescanning suffixes. |
| Org `org_link_2` scan @ `src/org_resolver.rs:2309`, owner `OrgInlineScan` @ `src/org_resolver.rs:93` | separate per-position successor memo | Org link-2 has a distinct page-like walk because `\` skips one full following character, not one byte. Its `PageRefScan` memo stores the close/no-close result for each classified position, excluding skipped escape-target bytes. String materialization (`name`, `full`, synthetic label) happens only after the close is known, so failed repeat probes share one charged scan and allocate nothing. |
| Org label reparse/full reconstruction @ `src/org_resolver.rs:1873` | bounded disjoint label span | The label string is reparsed once with `Ctx::label()`; the full-text first-Plain-only quirk reads only the first produced node. |
| Org link metadata @ `src/org_resolver.rs:2487`, owner `OrgInlineScan` @ `src/org_resolver.rs:93` | per-inline-buffer CR/LF `}` absence floor | Same placement as Markdown: it runs only after an accepted Org link and now matches mldoc's metadata stop set (`}`, CR, or LF). Metadata calls query nondecreasing positions within one buffer pass; a missing `}` charges one scan to CR/LF/EOF per line/buffer, later accepted links on that line reuse the absence result, and successes consume disjoint metadata spans. |
| Autolink parser body @ `src/inline.rs:1473` | invalidating-cursor owned by caller | parser may scan to `>`/ws, but dispatch only calls it after the shared boundary cursor succeeds. |
| Email parser body @ `src/inline.rs:1568` | suffix-absence miss-cache + invalidating-cursor | cached entry point owns both local `@` absence and domain boundary. |
| Bare URL path balance @ `src/inline.rs:1816`, `src/inline.rs:1826` | consume-on-match | the balanced tail is part of the emitted URL span and advances a single local cursor. |
| LaTeX backslash/dollar @ `src/inline.rs:2134`, `src/inline.rs:2155` | invalidating-cursor / `$`-before-eol invalidating-cursor + charged first-`$` body scan | Backslash closers are gated by resolver `\)`/`\]` cursors; top-level dollar dispatch is gated by `ByteBeforeEolScan`. The displayed `$$` body scan is the mldoc first-`$`/EOL scan, charged with `metrics::scan_work`; success consumes through the literal `$$`, and failure emits one `$` before re-dispatch at the next byte, so the failed displayed scan plus the fallback inline retry is bounded by a constant number of passes over that segment. Label reparses are bounded by the accepted label span. |
| Timestamp body slots @ `src/inline.rs:2843`, `src/inline.rs:3069`, token boundary cursor @ `src/inline.rs:300` | invalidating-cursor owned by caller | after a cursor-owned close candidate, delimiter-specific token-boundary cursors own the date/time/repetition slot scans; exact body spacing and two-slot interpretation are bounded local work with no `split_whitespace` suffix rescans or caps. |
| Raw HTML head @ `src/block_common.rs:686` | constant | known tag token scan is bounded by `MAX_HTML_TAG_LEN = 10`; the 10-byte `peek_string` gate is an O(1) horizon check. |
| Raw HTML special closer @ `src/block_common.rs:884`, `src/block_common.rs:950` | suffix-absence miss-cache | Accepted byte-closer scans consume through their matched closer and are charged; missing special closers update `RawHtmlScan.no_special_until`, so repeated misses in the same or smaller body window are O(1). |
| Raw HTML missing tag closer @ `src/block_common.rs:894`, `src/block_common.rs:950` | suffix-absence miss-cache | No `</tag>` ahead updates `RawHtmlScan.no_tag_end_until[index]`; transformed-view misses share this raw-coordinate floor after preserving the view peek horizon. |
| Raw HTML tag-index build @ `src/block_common.rs:620`, NSE @ `src/block_common.rs:708` | precomputed-map + exact-case cache + NSE | Each `RawHtmlScan` is tied to one input string. A queried exact-case tag token builds one combined pass over the whole input; opens stay exact-case and closes stay case-insensitive. The build then computes strict-below NSE entries with a charged monotone stack. Summation: `Σ builds <= T * n`, where `T` is the bounded universe of HICCUP-recognized exact-case ASCII tag tokens of length `<= 10`, so `T` is a parse-independent constant. |
| Raw HTML tag-index query @ `src/block_common.rs:774`, `src/block_common.rs:806` | NSE lookup + monotone cursors + direct fit checks | `after_tag` queries are non-decreasing per production `RawHtmlScan`, so per-tag event/close/self-close cursors advance at most once over their lists: `Σ cursor work <= events + queries`. The matching close is one strict-below NSE lookup from the event anchor, then `pos + close_len <= body_end`; self-close and Miss-kind use the first cursor position plus direct fit checks. Summation: one NSE build per queried tag (`O(events)`) + amortized monotone cursors + O(1) per-query checks. A non-production backward-query fallback is a charged linear walk. |
| Raw HTML self-close fallback @ `src/block_common.rs:828` | shared precomputed-map + direct fit check | `/>` positions are stored in the same per-tag index. Query work shares the monotone self-close cursor above and accepts only when the first self-close at or after `after_tag` satisfies `pos + 2 <= body_end`, so `Σ self-close work <= self-close events + queries`. |
| Raw HTML raw capture mapping/copy @ `src/block_common.rs:1078` | consume-on-match + charged copy | The raw helper first obtains a cached extent, then copies exactly `[opener, close_end)` and charges those bytes. Line mapping scans only the accepted extent and trailing blanks whose raw `Line.text` is empty; adjacent accepted captures are disjoint. |
| Raw HTML view capture mapping/copy @ `src/block_common.rs:1118` | raw-coordinate precomputed-map + charged accepted-view copy | The shared line view is a per-line slice decision with the mldoc exact-indent safe-sub no-op and a charged ltrim-prefix scan over mldoc's byte whitespace set (`' '`, `'\f'`, `'\n'`, `'\r'`, `'\t'`), bounded by the strip width or the line's own leading prefix. Non-all-ws and single-positive-frame lines stay on the cumulative fast path; nested all-ws lines use the documented segment-tree exception below. Raw tag/special/self-close events in kept regions still correspond 1:1 with view events. The helper preserves the view parser's 10-byte horizon by counting at most ten virtual body bytes, including one synthetic `\n` per source line that actually has an EOL, while clamping raw close queries to the last body line's raw content end. Accepted captures build only viewed lines and `\n` joins, charging each copied byte. Missing-close misses reuse `RawHtmlScan` floors; unbalanced tags route through the charged tag-index query path. |
| Markdown front matter prefix/body scans @ `src/parse.rs:80`, `src/parse.rs:106`, offset pass @ `src/parse.rs:155` | consume-on-match + charged body pass | The opening `---` check is constant; the closing fence search is charged to the searched prefix, and the directive body parser walks the accepted front-matter body once. If front matter is accepted, the remaining block tree receives one offset pass over its own AST nodes; no source suffix is rescanned. |
| Markdown HTML comment fallback @ `src/parse.rs:2604`, owner `Frame.md_html_comment_scan` @ `src/parse.rs:475` | per-frame suffix-absence miss-cache | The fallback `<!--`/`<!---` block scan is scoped to one exact line buffer, strip view, and `hi` by storing the floor on the active block `Frame`. A miss scans charged viewed lines once to `hi`, records "no trimmed `-->` from this line onward", and allocates no body string. Later opener lines in the same frame are O(1); successes materialize text only after the closer is found and consume through the closer line. Summation: one charged miss suffix per frame scope + disjoint successful captures. |
| Markdown footnote folding @ `src/parse.rs:2780`, helpers @ `src/parse.rs:2840`, `src/parse.rs:2861` | consume-on-match within block window | A footnote definition owns one forward cursor `j` over continuation lines within `hi`; each accepted continuation line is appended once into the transformed body buffer. The start/body helpers inspect only their current line slice. Σ over sibling footnotes is bounded by consumed lines plus copied body bytes. |
| Org footnote folding @ `src/org.rs:1493`, recognizers @ `src/org.rs:2630` | consume-on-match within block window | The parse branch owns one forward continuation cursor and appends each accepted body line once before reparsing the folded body. `footnote_def`/`footnote_cont` recognize only the current line; failed recognizer attempts do not scan later lines. |
| Org inline footnote definitions @ `src/org_resolver.rs:2505`, owner `OrgInlineScan` @ `src/org_resolver.rs:93` | CR/LF-bounded per-line `]` absence floor | Anonymous `[fn::def]` and named `[fn:name:def]` search for `]` only until the first CR or LF, matching the current reject byte set. A line with no `]` before CR/LF/EOF records that absence in the buffer's `ByteBeforeEolScan`, so repeated footnote candidates on the same line are O(1) after the first charged scan. Multi-line candidates reject at the first EOL without scanning to a later `]`; accepted definitions consume through their own `]`. |
| Shared block trim/space helpers @ `src/block_common.rs:386`, `src/block_common.rs:395`, `src/block_common.rs:422`, `src/block_common.rs:435` | bounded by caller-owned line/slice | These helpers scan only the line or slice already owned by the block parser branch that called them. In hot paths, callers advance linearly over disjoint lines or accepted transformed slices, so Σ trim/space bytes is bounded by the bytes of those caller-owned slices. |
| v2 source pass @ `src/v2/source.rs` | consumed source pass + sparse event-index build | `SourceScanner::scan` advances one cursor over the input and emits each line exactly once. Ordinary bytes are skipped with `memchr` to the next physical EOL or possible block hiccup opener. While a hiccup stack is open, the secondary `]`/`"` search is bounded to the bytes before the next primary source event instead of scanning the whole suffix from every `[:` opener, so `[:div ` x n plus one final `]` stays linear. Skipped slices are disjoint and charged once. During the same byte walk it records sparse hiccup opener/close pairs with one delimiter stack, so every matched `[:` opener receives at most one close byte; the pairs are sorted once by opener for later lookup, giving the documented `O(H log H)` exception where `H` is matched hiccup opener count. Per-line prefix metadata computes mldoc-space and OCaml-trim starts once for the completed line; event helpers then dispatch only on candidate first bytes (`#`, `:`, fence markers, or SUB for property `:END:`), so ordinary prose lines do not probe every event recognizer. Exact drawer `:END:` scans only candidate suffix trim bytes, property-prefix `:END:` and `#+END_` checks are prefix-only, and fence checks no longer require a full-line trim. `EndTrie::insert` touches each `#+END_` suffix byte once while building the index. Exact drawer `:END:` lines and property-prefix `:END:` lines are appended to sorted event vectors during this same pass. Line and block vectors are pre-sized from already-known input/line counts; this changes allocation constants only and does not add a scan. |
| v2 input-start front matter @ `src/v2/block.rs` | prefix close search + body line cursor + suffix AST offset | `markdown_front_matter_sequence` checks the opener in constant time, charges one search to the first later literal `---`, and parses the consumed body with one byte cursor over valid `key: value` lines. Invalid bodies return no directives after that same body pass, matching mldoc consumption. The suffix is parsed once by the normal v2 owner and then shifted by one AST walk; emitted suffix nodes are disjoint from the consumed front-matter prefix. |
| v2 leaf paragraph/directive/comment/HR slice @ `src/v2/block.rs` | source line cursor + borrowed/direct paragraph run buffer | `try_parse_leaf_blocks` advances over `Source.lines` once. The ordinary-paragraph guard accepts lines whose cached first non-mldoc-space byte is outside the union of block-start bytes; Markdown additionally scans only the current line for `::` and peeks one following line for definition-list openers before committing. Guard-accepted lines go straight into `ParagraphRun` and skip the full block-dispatch ladder. HR candidates are trimmed and marker-scanned once; directive and comment candidates scan only the current line prefix/body. Directive and Org-comment spans then advance over contiguous empty EOL-only lines for `between_eols`; Markdown line comments leave their trailing EOL in the active paragraph run, matching mldoc parser order. Paragraph/separator lines are accepted once into `ParagraphRun`. Runs whose bytes are already a contiguous LF/EOF source slice stay borrowed until flush; CR/CRLF normalization or non-contiguous tails materialize only the already accepted direct prefix once, then append transformed bytes. `Paragraph_Sep` still canonicalizes CR/LF exactly like mldoc. Lines that could start an earlier non-paragraph parser stop the paragraph slice and enter that parser's owner; if the owner is still unimplemented, v2 returns `None` rather than falling back. Inline parsing is called with the paragraph's absolute base, so direct paragraph/table/heading/list-term call sites no longer need a separate inline span-offset pass; origin-mapped transformed buffers still use their one monotone remap. |
| v2 top-level headings/bullets @ `src/v2/block.rs` | source line cursor + current-line title scan + bounded split suffix | Markdown ATX headings, Markdown `-` outline bullets, and Org `*` headlines inspect the current line for leading mldoc spaces, marker run, optional task marker, optional priority, optional Markdown bullet ATX size, and Org tag suffix. Empty-title trailing whitespace is appended once to the active paragraph run. Lone-CR heading lines reuse the same current-line scan; if no suffix block consumes the CR, the already-existing paragraph run owns that single EOL byte as a separator, so no suffix rescan is introduced. Hiccup-looking `[:tag]` title text stays in the title because mldoc disables block hiccup during heading-title lookahead, so it is just part of the one inline parse of the title. Heading-title colon ownership uses the concrete property/drawer start recognizer for Markdown, so rejected lone-colon titles such as Markdown `#\t:` stay in the same inline title parse after one current-line check; Org colon titles still enter the bounded suffix path because `Block.parse` can emit fixed-width examples. For document-level mldoc title-lookahead splits that v2 owns, same-line directive suffixes run the current-line directive parser first to mirror the normal post-heading parser order, property/drawer suffixes reuse the top-level property/drawer parser from the suffix offset with shared monotone closer cursors, HR suffixes are emitted from the current line only, table suffixes reuse the top-level table row/cell parser from the suffix offset and advance over accepted table/TBLFM lines once, empty/v2-safe callout suffixes reuse the top-level callout owner from the suffix byte, closed displayed-math suffixes use one accepted-span close scan and then either return a paragraph/HR tail or recurse only through the same bounded suffix helper for directive, property/drawer, table, LaTeX, raw-HTML, blockquote, footnote, fence, and special-body tails, blockquote suffixes reuse the top-level quote collector from the suffix offset and then parse a disjoint body buffer, footnote suffixes reuse the top-level marker/body parser from the suffix offset and advance over accepted continuation lines once, LaTeX environment suffixes reuse the top-level KMP close scan and hand same-line tails to the paragraph runner, raw-HTML suffix sequences reuse `RawHtmlScan`'s cached per-tag indexes and advance across disjoint sibling captures before handing paragraph/HR tails back to the caller, closed fence suffixes use the source fence index plus the shared monotone closer cursor, and closed `SRC`/`EXAMPLE`/`EXPORT`/`COMMENT` suffixes use the source-built `#+END_` trie plus one accepted body copy, exactly like the top-level directive, property/drawer, table, math, blockquote, callout, footnote, LaTeX, raw-HTML, fence, and special body-block rows. Rejected title suffixes reuse the same raw-HTML, math, table row/separator, footnote-definition marker/body, Markdown property-line, unclosed Markdown drawer close-absence probe, directive/property-line, fence, blockquote, LaTeX, and `#+BEGIN_` recognizers as other same-line tail paths before emitting one inline title; the property/directive probes are current-line checks after broad start guards and valid lines keep their normal suffix paths. Bounded suffix recursion uses this same decision for heading/bullet suffixes after accepted property, math, raw-HTML, and hiccup spans. Nested block-content/list-content reparses use a context flag that disables heading-title callout suffixes, so suppressed `- #+BEGIN_*` bodies remain paragraph text without an extra pass. Other non-empty title suffixes that would trigger still-unowned block families return `None` before committing the heading. |
| v2 heading marker/priority edge fields @ `src/v2/block.rs` | current-line field scan | `split_heading_markers` scans the current line exactly once across the mldoc `ws *> marker`, `ws *> priority`, and `ws *> title` positions. Missing field whitespace records one same-line paragraph tail offset instead of reparsing the suffix. Empty-marker drop checks inspect only the next nonblank line with current-line recognizers, and Org `#+...` drop tests use concrete drawer/directive openings rather than a broad prefix scan. Heading-title directive probes stop at the first `:` and reject mldoc spaces in the name, so malformed space-containing names fall through to the single inline title parse. |
| v2 top-level drawers/properties @ `src/v2/block.rs` | source line cursor + monotone closer cursors + bounded split suffix | Markdown property groups and standalone parse2 fallback lines scan each accepted property/directive line once. Rejected top-level Markdown property-looking lines reuse the same current-line concrete property parser after the broad start guard fires; valid property lines keep their normal path and rejected ones enter the paragraph run for that line. Lone-CR Markdown parse1 property candidates stop the current property fold and are consumed by the normal paragraph run, while parse2 `#+KEY:` lone-CR lines remain accepted exactly once and leave the post-CR tail to the line cursor. Parse2-owned blank separators are consumed once by the property fold; parse1-only blank separators are left to the paragraph runner except when the next nonblank line is a parse2 property, matching mldoc's fold. `:PROPERTIES:` and generic drawers query sorted exact/prefix `:END:` event vectors with monotone cursors; each closer index is skipped at most once. If a drawer has no later close or its candidate span contains a lone-CR line, the miss is final for that opener and the normal block ladder owns the same current line as paragraph/fixed-width fall-through. Drawer body property lines are walked over the accepted body interval once. The same parser is reused for bounded property/drawer suffixes after headings, displayed math, and hiccup blocks with the caller's shared monotone closer cursors. Same-line `:END:` tails that bounded-split to a property/directive node are folded into the same property block in place; other same-line tails either become one paragraph/comment/heading/list/Org fixed-width tail or reuse the bounded suffix helper for property/drawer, table, LaTeX, raw-HTML, blockquote, empty/v2-safe callout, footnote, displayed-math, fence, and special-body tails. Malformed raw-HTML-looking tails reuse the shared raw-HTML reject cache; unclosed math and fence tails use the same close/fence indexes as their top-level parsers; rejected table-looking tails reuse the table row/separator checks that already declined the bounded split; rejected regular-list marker tails reuse the current-line marker parser that already declined a marker without a same-line body; unclosed Markdown drawer-looking tails binary-search the source `:END:` event vector for close absence; rejected Markdown property-looking tails and malformed non-`BEGIN_` `#+...` directive/property-looking tails reuse the concrete current-line property parsers after the broad start guard fires, so valid property/directive lines keep their normal paths; rejected `>` tails use the blockquote parser's local `Paragraph` decision; malformed LaTeX and unclosed/malformed `#+BEGIN_` tails reuse the same bounded recognizers as their top-level rows. Each rejected form then becomes a single paragraph tail. Callout suffixes reuse the top-level callout owner from the suffix byte and copy only the accepted body interval; unsafe closed bodies still return `None` before the drawer commits. Those suffixes consume disjoint bytes after the close marker and use the same scan owners as their top-level rows; tails requiring unowned families return `None` before committing the drawer. |
| v2 top-level tables @ `src/v2/block.rs` | source line cursor + row/cell split | The table group loop advances one line cursor over accepted row, separator, and optional non-empty `#+TBLFM:` lines. Each row is split into cells once after mldoc/OCaml trimming; each accepted cell is inline-parsed once and span-offset in place. Separator align enrichment is markdown-only and scans only the accepted separator line. Malformed pipe candidates are rejected by the current-line row/separator checks and then appended once by the paragraph runner. Lone-CR row tails and TBLFM EOL tails are appended once to the paragraph run before the cursor advances. |
| v2 top-level LaTeX environments @ `src/v2/block.rs` | source line cursor + KMP close scan | The opener/name scan follows mldoc `latex_env.ml`: leading mldoc spaces, non-empty name through the first `}`, and `spaces_or_eols` before content. Malformed top-level openers stop after that bounded name scan and return the current line to the paragraph runner. The generated case-insensitive `\end{name}` pattern is searched with a prefix table, so long environment names and many body backslashes stay O(name + body). Accepted top-level environments advance the line cursor to the first line start at/after `consumed_end`; any suffix/newline before that line start is appended once to the active paragraph run. Bounded split-suffix calls reuse the same capture helper from the suffix start and return any same-line tail to the caller's paragraph runner, so no additional suffix reparse is introduced. |
| v2 top-level fenced code @ `src/v2/block.rs` | source fence index + monotone closer cursor | The source pass indexes OCaml-trimmed whole-line fence markers once. `fence_sequence` reaches openers in source order and uses one shared `find_matching_fence` cursor, so each indexed marker is skipped at most once. Accepted body text is rebuilt from accepted body lines with one synthetic `\n` per line, normalizing CRLF without copying raw EOL bytes; language scans only the opener info suffix, and trailing blank swallow advances over each blank line once. Lone-CR opener/body candidates and unclosed fence candidates fall through to the paragraph runner after the cursor proves rejection; each rejected candidate line is then appended once like any other paragraph line. A lone-CR closer is consumed as the closer and does not add an extra body scan. |
| v2 top-level special body blocks @ `src/v2/block.rs` | source `#+END_` trie + monotone closer cursor | The source pass inserts every `#+END_` suffix into a prefix trie once. `raw_src_example_sequence` owns `SRC`, `EXAMPLE`, `EXPORT`, and `COMMENT`: it scans only the opener name, then uses the trie's per-prefix monotone cursor to find the first later closer; each closer index advances at most once for a prefix. The accepted body interval is collected once for `block_code_texts`, which clears first-body indent in one pass over those body lines; `SRC` then scans the first option token once, `EXPORT` splits backend/options once over the opener tail, and `COMMENT` is opaque. Trailing blank swallow advances over each blank once. `#+BEGIN_*` lines with no parseable name or no valid later closer fall through to the paragraph runner after one opener-name probe and one trie miss. |
| v2 top-level callout containers, empty and v2-safe block-content @ `src/v2/block.rs` | source `#+END_` trie + frame stack + body origin map | Empty `#+BEGIN_QUOTE` and non-special custom callouts reuse the source-built `#+END_` trie as raw blocks. Clean nested callout/paragraph/raw-HTML bodies use `try_parse_unindented_callout_body`: the source line cursor advances once through the accepted body range, each nested callout pushes one heap frame and pops once at its matching `#+END_`, each popped frame trims only its immediate paragraph-break boundary before attachment, and active clear-indent increments are queried through `StripSeqTree`, giving the documented `O((frames + all-ws bytes) log depth)` exception for exact all-whitespace sequential clearing. Unstripped same-line raw-HTML children are parsed with a bounded line-local raw-HTML recognizer so short callout siblings do not rebuild per-tag global indexes. Whitespace-only body lines remain in the current paragraph run instead of causing recursive fallback. Other owned non-empty bodies copy each accepted clear-indent body line once into the transformed body and record an `OriginMap`; the nested v2 parse owns that disjoint buffer, then mldoc-suppressed body constructs (`Heading`/Logseq `Bullet`, Markdown property/drawer spans, and Org generic drawer spans) are rewritten over their original transformed spans. Footnote definitions are document-parser constructs, so footnote-looking block-content lines stay in the paragraph run and following fences/blocks keep their normal block-content precedence. Markdown property/drawer spans are scanned once more over that accepted span and split back into the block-content result that would have won without `Drawer.parse`: paragraph text for parse1/drawer lines and `Directive` children for parse2 lines. When such a span starts with `>`, the accepted span is parsed once through the bounded Markdown blockquote owner instead, and descendant quote span starts are normalized over that already-emitted subtree to match mldoc projection. Org property drawers are rewritten over the same span into an `Example` by scanning each accepted colon line once, matching the block-content verbatim branch. Org generic drawers are scanned once over the accepted drawer span and decomposed into fixed-width example runs plus intervening block-content chunks; those chunks are disjoint slices reparsed through the same v2-owned block-content path. Adjacent rewritten paragraphs and adjacent colon-source rewritten examples are merged, general transformed-body paths trim paragraph breaks before child blocks with one AST walk, while the clean callout frame path performs only the shallow per-frame trim described above and never recurses through a completed nested-callout tree. Inline remapping walks with a monotone cursor. Child block-span remapping uses a binary-search helper over origin segments (`O(B log S)` for `B` child blocks and `S` source-map segments) so stripped-body public spans do not require a cursor rewind; transformed paragraph block spans are cleared to match the existing projection while inline and leaf-block spans remain absolute. Body outputs containing regular-list items needing omitted list-content parsers or still-unowned nested constructs return `None` before committing the container. |
| v2 Markdown `>` blockquotes with v2-safe block-content @ `src/v2/block.rs` | source line cursor + quote frame stack + body origin map | `markdown_blockquote_sequence` consumes one opener line, or a bounded suffix opener from a known byte offset, and advances over accepted continuation lines once. Each line uses the mldoc `md_blockquote` stripping rule over the current line slice only, appends a synthetic `\n`, and records source slices/EOLs in an `OriginMap`. Quote-only bodies use `try_parse_quote_only_body`: every physical body line is passed through each still-active quote frame at most once until a continuation fails, failed breaker lines close frames and are retried at the surviving parent view, and each frame is popped once with paragraph-break trimming before it is attached to its parent. Paragraph bytes are copied once into mapped paragraph buffers and remapped to the original quote body; there is no recursive body materialization for quote staircases, `gt_spine`, or `gt_breaker`. Other bodies use the nested v2 parse over the disjoint body buffer and share the callout block-content suppression path; heading-looking split lines in block-content are appended once to the active paragraph run instead of invoking document-level split suffix parsing. Following true-empty lines are skipped once without extending the quote span. Regular quote bodies remap child block spans through the origin map and clear paragraph block spans. Direct Org generic drawer rewrites preserve local child block spans and remap only inline spans with a monotone origin cursor, matching current quote projection without adding another body pass. Rejected top-level quote triggers are pushed into the active paragraph run; rejected suffix quote triggers return `None` before committing the preceding split. Bodies requiring still-unowned block-content parsing return `None` before committing the quote. |
| v2 Org fixed-width examples @ `src/v2/block.rs` | source line cursor + example run buffer | `org_verbatim_sequence` runs only after drawer/property precedence has failed. It advances the shared line cursor over accepted `:` lines, appending each line's post-marker suffix once and stopping run coalescing at lone CR to match mldoc's `eol` parser. LF/CRLF blank separators immediately after an accepted run are consumed once into the `Example` span without code text, then the current run stops so a later `:` line starts a new example. Unclosed drawer-looking lines are a fallthrough to this branch rather than a suffix search, so each line is still owned by one forward pass. |
| v2 footnote definitions @ `src/v2/block.rs` | source line cursor + footnote body origin map | `footnote_sequence` owns one accepted document-context definition at a time after generic block parsing, and bounded suffix callers use the same parser with a suffix byte offset. Block-content and list-content contexts skip this parser, matching mldoc parser groups and preventing footnote-looking paragraph lines from absorbing following fences or blocks. The marker parser scans only the first line slice; the body loop advances over accepted continuation lines until the first mldoc footnote terminator. Each copied body slice and synthetic line joiner is appended once to an `OriginMap`, then inline remapping walks the emitted inline tree with a monotone source-map cursor. Trailing blank absorption advances over each consumed blank line once. A rejected following Org regular-list-looking terminator such as an indented `-` line reuses the generic current-line marker probe and is appended once by the paragraph runner. Suffix forms are disjoint from the preceding block span, so they add no second pass over prior bytes. |
| v2 regular lists with v2-owned list-content blocks @ `src/v2/block.rs` | source line cursor + per-item origin maps + item stack | `regular_list_sequence` runs before Markdown definition lists. It advances one cursor over every accepted marker, folded continuation, and consumed true-empty `two_eols` line. Each item body appends OCaml-trimmed source slices and synthetic joiners once to a local `OriginMap`; the list-content v2 parse owns that disjoint item buffer, then heading/bullet, regular-list-looking body lines, Markdown property/drawer blocks, `Directive` blocks, parse2 `#+KEY:` property-looking blocks, and Org generic drawer spans that list-content grammar omits are rewritten over their existing spans according to the parent parser context. Footnote-looking item lines remain paragraph text because list-content skips the document footnote parser. Markdown property/drawer and parse2 `#+KEY:` blocks become paragraph text in document-list content; `Directive` blocks are suppressed in document-list content but preserved in block-content list items and in quote/custom children, where mldoc's block-content parser includes directives. Regular-list-looking item body lines and the Markdown definition-list parser are disabled in `ListContent`, matching `mldoc_parser.ml`'s top-level list-content parser set and leaving definition/list-looking item text in the paragraph run; the case-sensitive `#+RESULTS:` marker is recognized by one current-line prefix scan only in document regular-list item content, emits one `block:Results` leaf, and hands same-line suffix bytes or the unconsumed EOL to the same paragraph runner that owns ordinary list-content text. In block-content regular-list item content, the same bytes follow the directive parser and are preserved as `Directive`. Org property drawers become an `Example` by scanning each accepted colon line once; Org generic drawers are scanned once over the accepted span and decomposed into fixed-width example runs plus disjoint intervening list-content chunks. Adjacent rewritten paragraphs and adjacent colon-source rewritten examples are merged, paragraph breaks before following item-content blocks are trimmed, and inline nodes are remapped once while preserving mldoc/current local item-content block spans. Mldoc's loose `check_listitem` child triggers, including leading-integer text and rejected indented Org `-` child markers, are detected during the same continuation scan; the iterative scope stack records the recursive parser's rollback floor, so a failed first child collapses to its parent floor while failed later siblings truncate only the already-built flat suffix once before returning to the document paragraph path. `nest_items_with_boundaries` folds the flat items iteratively with an explicit stack. Item bodies that would require omitted list-content parsers return `None` before committing the list. Top-level regular-list-looking markers whose concrete marker parser rejects for empty body reuse the current-line marker parser and enter the paragraph path. Latest-mldoc lone-CR list markers are kept in the paragraph path, avoiding fallback to current lsdoc's version-skewed list behavior. |
| v2 Markdown definition lists @ `src/v2/block.rs` | source line cursor + definition body origin map | `markdown_definition_sequence` runs after regular lists, footnotes, and generic block parsing, before HR. The list cursor consumes each term, `: body`, continuation, and absorbed blank line once. Each definition paragraph builds one local `OriginMap` from copied body slices and synthetic joiners, then remaps the emitted inline tree with a monotone cursor. Committed definition-list terms continue inside the same cursor without re-running the block ladder. |
| v2 top-level displayed math @ `src/v2/block.rs` | source line cursor + accepted-span close scan + bounded split suffix | Each owned `$$` opener calls the shared mldoc-style close scan over the remaining input only until an accepted close or confirmed absence. Successful captures advance to a disjoint close/tail position; adjacent same-line math captures rewrite to the unconsumed suffix, while same-line property/drawer, table, block hiccup, LaTeX, raw-HTML, blockquote, empty/v2-safe callout, footnote, fence, and special-body tails reuse the same bounded suffix helper and cursors documented for heading/hiccup tails. Unclosed top-level openers are handed to the paragraph runner for the current line; unclosed same-line suffixes after an accepted capture are emitted as a paragraph tail. This can scan the rejected suffix once for close absence and once as paragraph text, still bounded by a documented two-pass line-local fallback. Malformed raw-HTML, rejected table-looking rows/separators, rejected regular-list marker tails with no same-line body, rejected footnote-definition-looking tails, unclosed Markdown drawer-looking tails, rejected Markdown property-looking tails, malformed non-`BEGIN_` `#+...` directive/property-looking tails, unclosed fence, rejected blockquote, malformed LaTeX, and unclosed/malformed `#+BEGIN_` tails reuse the same bounded recognizers as the drawer/property tail path before becoming one paragraph tail. Callout suffixes reuse the source `#+END_` trie and body origin map documented in the callout row, and unsafe closed bodies return `None` before the math capture is committed. Paragraph/HR tails are handed back to the caller once; trailing blank absorption advances the line cursor over each blank line once. Tails needing unowned block families return `None` after one failed suffix decision. |
| v2 top-level raw HTML @ `src/v2/block.rs` | shared raw-HTML indexes + source line cursor + bounded split suffix | `raw_html_sequence` runs at mldoc's block precedence point after displayed math and before hiccup. Each accepted capture uses `RawHtmlScan`'s miss floors and per-tag indexes, then copies only the accepted raw `text`; adjacent same-line captures advance over disjoint suffixes in one local loop. Whole-line captures absorb each trailing blank line once, and invalid candidates fall through without rescanning. Accepted same-line tails reuse the bounded split suffix helper for comment, heading/list, property/drawer, table, block hiccup, displayed-math, LaTeX, raw-HTML, blockquote, regular-list, empty/v2-safe callout, Org fixed-width, footnote, fence, and special-body tails, sharing the caller's monotone drawer/property/fence cursors and raw-HTML scan. Malformed raw-HTML, rejected table-looking rows/separators, rejected regular-list marker tails with no same-line body, rejected footnote-definition-looking tails, unclosed Markdown drawer-looking tails, rejected Markdown property-looking tails, malformed non-`BEGIN_` `#+...` directive/property-looking tails, unclosed math/fence, rejected blockquote, malformed LaTeX, and unclosed/malformed `#+BEGIN_` suffixes reuse the same bounded recognizers as the drawer/property and displayed-math tail paths before becoming one paragraph tail. Callout suffixes reuse the top-level callout owner from the suffix byte and remain disjoint from accepted raw-HTML spans. Paragraph tails are appended once by the caller, so same-line raw-HTML suffix handling is still cursor-forward. |
| v2 top-level block hiccup @ `src/v2/block.rs` | sparse source hiccup close index + source line cursor + bounded split suffix | The source pass pre-pairs every matched `[:` opener with its matching `]` using mldoc's string-aware stack rule, records only matched openers, then sorts that sparse pair list once. `hiccup_sequence` first checks the current opener bytes and head in place, then performs one binary close lookup, copies only the accepted raw `v`, and advances either to a disjoint same-line tail or to a later source line. Same-line comment, heading/list, HR, property/drawer, closed displayed-math, LaTeX environment, raw-HTML sequence, blockquote, regular-list, empty/v2-safe callout, Org fixed-width, footnote definition, closed fence, and closed `SRC`/`EXAMPLE`/`EXPORT`/`COMMENT` tails reuse the bounded split suffix helper; accepted property/drawer groups and math, LaTeX, raw-HTML, blockquote, regular-list, callout, footnote, fence, and body copies are charged by their own top-level rows. Malformed raw-HTML, rejected table-looking rows/separators, rejected regular-list marker tails with no same-line body, rejected footnote-definition-looking tails, unclosed Markdown drawer-looking tails, rejected Markdown property-looking tails, malformed non-`BEGIN_` `#+...` directive/property-looking tails, unclosed math/fence, rejected blockquote, malformed LaTeX, and unclosed/malformed `#+BEGIN_` suffixes reuse those same bounded reject recognizers before the caller appends one paragraph tail. Paragraph/adjacent-hiccup/split-helper tails advance disjointly, whole-line captures absorb each trailing blank line once, and invalid heads fall through without suffix rescans. |
| Timestamp `skip_mldoc_spaces` / token cursors @ `src/inline.rs` | bounded timestamp body slot | The helper is called only inside a timestamp body after a delimiter cursor has found the body end. Delimiter cursors search only for the active close byte; token-boundary cursors stop at close or mldoc spaces, not CR/LF, matching mldoc `non_spaces`. Each cursor is monotone per inline buffer and per delimiter kind. Range parsing uses cloned cursors transactionally and commits only on success, so fallback does not rescan an unbounded suffix. |
| Markdown synthetic-emphasis unescape @ `src/resolver.rs:791`, `src/resolver.rs:833` | bounded accepted emphasis subtree | The synthetic nested-emphasis repair maps over the already accepted subtree once. Plain children use their own span to recover the raw slice and `markdown_plain_text` charges that slice; concat then touches only the repaired children. Σ work is bounded by the accepted emphasis body bytes. |
| OriginCursor monotonicity tripwire @ `src/source_map.rs:179`, assert @ `src/source_map.rs:189` | debug invariant for monotone owner rows | `advance_to` binds the cursor to one map and debug-asserts that callers never query behind the last skipped segment. The assertion documents and enforces the monotone-cursor precondition used by the source-map remap rows above; no residual missing-assert risk remains. |

## 2026-07-09 shortcut audit amendments

- `markdown_fast_simple_bold` performs one bounded body scan only after a candidate
  closing `**` has been found. Accepted simple-strong spans are disjoint; rejected
  context-sensitive candidates fall through to the cached source-transcribed
  nested-emphasis parser. The worst case is therefore the existing current-buffer
  fast scan plus one fallback resolver pass for the same buffer, not an unbounded
  retry family.
- The added quote/callout guards for `*` and `_` are constant-time checks on the
  current stripped line. They reduce shortcut ownership and do not add a scan.
- Full trailing-separator trimming before structural child blocks pops each
  `Inline::Break` at most once during the existing AST trim walk. Preserving the
  Markdown line-comment exception is a branch on the already-adjacent child block.
- Transformed-body span remapping stays on the existing owners: inline spans use a
  monotone `OriginCursor`; block spans use the documented origin-segment binary
  search helper when a cursor would rewind; paragraph block-span clearing is an AST
  mutation over already-emitted children; and the direct Org generic-drawer rewrite
  remaps only inlines, preserving local block spans without another body pass.
- Top-level vs nested paragraph separator ownership is carried by the existing line
  cursor. A paragraph break is either appended once to the active paragraph run
  (document context) or popped once before an accepted structural child block
  (block/list content). The Markdown-comment and LaTeX exceptions are single
  parser-order branches on already-classified child blocks.
- Empty-marker fence-tail handling uses the existing fence close index/closer cursor.
  A marker-looking ``` or `~~~` line is not enough to trigger a tail drop; the helper
  checks that the following fence would actually be accepted. Unclosed candidates
  therefore fall through to paragraph ownership without adding a new suffix scan.
- Timestamp fallback commits only delimiter-absence facts from failed general
  attempts and keeps full cursor commits for successful range/general parses. The
  date-start prefilter is a constant byte check before close search, so malformed
  timestamp-looking prose cannot force repeated close scans.
- Markdown link label `[` handling is owned by `MdLinkScan`'s label table. A malformed
  `[` now fails the candidate with O(1) table/memo queries instead of scanning forward
  as plain label text; many-openers/one-close inputs share the per-buffer table.
- Org link-label and link2 fixes reuse `OrgInlineScan`'s existing label/link2
  successor memos. Changing the accepted grammar/context does not add a new scan
  owner.

Documented non-inline exceptions:

| exception @ file:line | bound | note |
|---|---|---|
| refs extraction and sort @ `src/refs.rs` | `O(AST nodes + inline nodes + property value bytes) + O(R log R)` | `extract_refs` uses explicit stacks for blocks/list items and inline child slices, so deep `Custom`/`Quote`/list/inline trees do not consume native recursion. Parse1 property values are reparsed once according to their format with mldoc's `inline_skip_macro = true`, then only the top-level `Tag`/`Link`/`Nested_link` candidates are inspected; parse2 properties are skipped. Sorting/deduplication is the remaining `O(R log R)` exception, with reference count `R <= input bytes`, outside inline parsing. |
| sparse hiccup close indexes @ `src/v2/source.rs`, `src/inline.rs` | `O(H log H)` | `H` is the number of matched hiccup openers recorded by the source pass or by the inline fast path for one inline buffer, and `H <= input bytes`. Each sort replaces a dense input-sized close table, avoiding an allocation proportional to source bytes for documents with no or few hiccups. |
| GT fallback recursion cap @ `src/block_common.rs:33` | capped | `GT_FALLBACK_NEST_CAP = 64` applies only to the residual transformed quote fallback. |
| sequential all-ws clear-indent tree @ `src/block_common.rs:98`, query @ `src/block_common.rs:166` | `O((frames + all-ws bytes) log depth)` | Exact mldoc sequential clear-indents for all-whitespace lines under two or more positive strip increments use a Vec-backed min segment tree over the active positive increment stack. `Σ tree work ≤ (pushes + pops)·log depth + Σ_ws-lines (1 + subtractions)·log depth`; every successful hop subtracts at least one byte, so `subtractions ≤ line length`, giving `O((frames + all-ws bytes)·log depth)`. All pushes, pops, and descent nodes are charged with `scan_work`; zero increments are excluded. |
